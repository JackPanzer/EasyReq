CREATE TABLE CASOUSO (ID BIGINT NOT NULL, PRECONDICION VARCHAR(255), POSTCONDICION VARCHAR(255), FRECUENCIA VARCHAR(255), COMENTARIO VARCHAR(255), DETALLE VARCHAR(255), PRIINICIO VARCHAR(255), EVENTO VARCHAR(255), PRIELABORACION VARCHAR(255), UNIDAD VARCHAR(255), PRICONSTRUCCION VARCHAR(255), NOMBRE VARCHAR(255), ABSTRACTO SMALLINT DEFAULT 0, PRIMARY KEY (ID))
CREATE TABLE PASOEXCEPCION (ID BIGINT NOT NULL, Numero_paso INTEGER, TIPO SMALLINT DEFAULT 0, DESCRIPCION VARCHAR(255), PERTENECE_ID BIGINT, PRIMARY KEY (ID))
ALTER TABLE PASOEXCEPCION ADD CONSTRAINT PSXCEPCIONPRTNCEID FOREIGN KEY (PERTENECE_ID) REFERENCES CASOUSO (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
